#include <stdio.h>
int main()
{
    int rows,columns,i,j,k;
    printf("Enter the number of rows:");
    scanf("%d",&rows);

    columns = rows + 4-1;

    for (i=1;i<=rows;i++){
        for(k=columns;k>=1;k--){
            printf(" ");
        }
        for(j=1;j<=i;j++){
            printf("* ");
        }
        printf("\n");
        columns--;
    }
}


OUTPUT :

Enter the number of rows:5
        * 
       * * 
      * * * 
     * * * * 
    * * * * * 
